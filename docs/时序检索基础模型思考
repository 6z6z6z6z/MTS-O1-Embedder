时间序列基础检索表征模型的目标，不只是压缩和迁移，而是为时序数据建立一个可检索的语义空间。
问题定义
最关键的是先把“检索到底检索什么”定义清楚。因为时间序列的相似性不是单一的。至少可能有几种不同的相似性：
一种是形态相似，比如波形、局部 pattern、频率结构接近。
 一种是趋势机制相似，比如都呈现上升后震荡、突变后恢复、周期性增强这类动态演化规律。
 一种是语义相似，比如虽然数值尺度不同，但都对应某种共同场景，例如节假日负荷波动、围术期血压下降前的先兆模式、光伏受云层扰动时的短时塌陷。
 还有一种是任务相关相似，即对某个特定任务最有帮助的“可迁移邻居”。

所以你这个基础表征模型，最好不要只学一种单一 embedding，而是考虑做成多粒度、多视角的表示体系。比如同时保留局部 patch-level 表征、全局 sequence-level 表征、趋势/周期分解后的结构表征，以及可选的上下文条件表示。这样检索时才能灵活支持不同粒度的相似性需求。
几个维度
第一个层面是表示对象。你到底编码什么？是原始时间序列，还是趋势项、周期项、残差项，还是加上协变量、事件、文本描述后的多模态时间序列单元。这个决定了你的模型最终能否支持“语义级”检索，而不是只能做波形匹配。
第二个层面是预训练目标。如果要为检索服务，光靠 reconstruction 往往不够。你需要显式塑造 embedding space。这里可以重点考虑对比学习、instance discrimination、prototype alignment、跨尺度一致性学习、时间增强一致性学习，以及你之前感兴趣的离散 token 学习。也就是说，模型不仅要“能压缩”，更要“压缩后可比较、可索引”。
第三个层面是检索评测基准。这是很重要的一点。因为如果没有一个像样的 retrieval benchmark，这类工作很容易退化成“顺便做了个 embedding”。你可以考虑构建几类检索任务，比如：
 给定一段查询序列，检索形态最相近的历史片段；
 给定一个当前场景，检索未来演化模式最接近的 case；
 给定带上下文的多变量序列，检索相同机制下的跨域样本；
 再进一步，做 retrieval-augmented forecasting，验证检索到的记忆是否真的提升预测质量。

研究挑战：
- 一个难点是时间序列的“语义”不像文本那么显式，很多语义依赖上下文。脱离领域场景，只看数值，模型学到的可能只是形状相似，而不是任务真正关心的机制相似。所以后续可能要把context-aware representation 纳入进来。
- 另一个难点是检索标签不天然存在。文本可以靠语义近邻，图像可以靠类别近邻，但时间序列什么算正样本、什么算负样本，并不总是明确。所以正负样本构造、相似性定义、层级监督会成为关键。
- 再一个难点是跨域统一。不同领域的采样率、量纲、噪声模式、周期结构差别很大。如果你想做“通用基础模型”，就需要在归一化、时间尺度对齐、频域建模、多尺度编码这些地方下功夫。


相关工作调研
- Gemini Embedding
- BGE Embedding


